# GENERATED by themes/wildcharm/render.py from palette.yml v0.7.1 + scopes.yml — do not edit; edit the sources and re-render
# swaynag — wildcharm dialogs, light (config-dark / config-light pair; the
# bootstrap symlinks the `theme_variant` one to ~/.config/swaynag/config).
# Values are bare RRGGBB (no '#'); annotations on their own lines.
#
# GOTCHA: swaynag's built-in `warning`/`error` types ship hardcoded colours and
# do NOT layer the global options — only keys set INSIDE a type's [section]
# override its presets, so the theme is restated in full per section.

# --- global / default (dialogs with no -t) ---
font=Lilex Nerd Font Mono 11
background=f5f5f5
border=c6c6c6
border-bottom=d7005f
border-bottom-size=2
text=1a1a1a
button-background=ffffff
button-text=1a1a1a
details-background=ffffff

# --- warning (the sway exit confirm passes -t warning): yellow accent line ---
[warning]
background=f5f5f5
border=c6c6c6
border-bottom=d78700
border-bottom-size=2
text=1a1a1a
button-background=ffffff
button-text=1a1a1a
details-background=ffffff

# --- error: bright-red accent line ---
[error]
background=f5f5f5
border=c6c6c6
border-bottom=d70000
border-bottom-size=2
text=1a1a1a
button-background=ffffff
button-text=1a1a1a
details-background=ffffff
