# glow config — symlinked to ~/.config/glow/glow.yml
# Custom wildcharm/#ce0056 style; absolute path (glow does not expand ~).
# Update this path if the repo moves (mirrors the waybar gpu.sh convention).
style: "/home/dimitrios/.config/glow/wildcharm.json"
# mouse support (TUI-mode only)
mouse: false
# use pager to display markdown (vim :Glow and vifm pass -p explicitly anyway)
pager: false
# word-wrap at width
width: 80
