" hestia theme variant selector — light. The bootstrap symlinks this (or the
" -dark sibling) to ~/.vim/hestia-background.vim per `theme_variant` (M7);
" .vimrc sources it before `colo hestia`. Hand-written (no colours) — not a
" render.py artifact.
set background=light
